version: '3.8'

services:
  review-bot:
    build: .
    ports:
      - "${PORT:-80}:${PORT:-80}"
    volumes:
      - ./.env:/app/.env:ro
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
